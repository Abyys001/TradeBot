//@version=5
strategy("readback")
var line trend = na
if bar_index == 1
    trend := line.new(bar_index, high, bar_index, low)
level = line.get_price(trend, bar_index)
if close > level
    strategy.entry("L", strategy.long)
//@expect code=drawing_readback line=6 col=9
